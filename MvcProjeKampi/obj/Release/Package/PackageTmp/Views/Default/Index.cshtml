@using EntityLayer.Concrete
@using PagedList
@using PagedList.Mvc
@model PagedList.IPagedList<Content>
<br />
<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.HeadingName - MVC Proje Kampı</title>
</head>
<body class="hold-transition sidebar-mini">
    <div class="wrapper">
        <section class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-md-12">
                        <div class="timeline">
                            <h2 class="text-center">@ViewBag.HeadingName</h2>
                            @foreach (var item in Model)
                            {
                                <div class="time-label">
                                    <span class="bg-success">
                                        @(((DateTime)item.ContentDate).ToString("dd-MMM-yyyy"))
                                    </span>
                                </div>
                                <div>
                                    <i class="fas fa-comment bg-yellow"></i>
                                    <div class="timeline-item">
                                        <h3 class="timeline-header"><a href="#">@item.Writer.WriterName @item.Writer.WriterSurName</a> </h3>
                                        <div class="timeline-body">
                                            @item.ContentValue
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                        <a href="/WriterPanelContent/AddContent/@ViewBag.ID" class="btn btn-primary col-md-6 offset-md-3">Entry Yaz</a>
                        <br />
                    </div>
                </div>
            </div>
        </section>
        <link rel="stylesheet" href="~/Content/PagedList.css" />
        @if (ViewBag.EntryCount > 5)
        {
            @Html.PagedListPager((IPagedList)Model, page => Url.Action("headings", new { page }))
        }
    </div>
</body>
</html>

